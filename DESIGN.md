# Design System: KOALA — 한국 작가 미술품 마켓

이 문서는 새 화면을 만들 때 참고하는 단일 기준이다. 값은 전부 `src/styles/theme.css`
와 `src/styles/fonts.css` 에서 가져온 실제 토큰이다. 여기 없는 색과 글꼴은 쓰지 않는다.

---

## 1. 분위기

작품을 거는 자리다. 화면이 작품보다 먼저 눈에 띄면 진 것이다.

바탕은 비우고, 사진은 크게, 글자는 조용하게 둔다. 미술관 도록에 가깝고
쇼핑몰에서 멀다. 상품이 15점뿐이라 빽빽하게 채울 이유도 없다.

- **밀도 3 / 10** — 화랑처럼 성기게. 한 화면에 무엇을 더 넣을지보다 무엇을 뺄지 먼저 본다
- **변주 6 / 10** — 원작 섹션은 앞의 둘을 크게, 나머지를 작게 건다. 같은 크기로 늘어놓지 않는다
- **모션 4 / 10** — 스프링을 쓰되 절제한다. 작품 사진이 흔들리면 값이 싸 보인다

---

## 2. 색

### 브랜드

| 이름 | 값 | 쓰임 |
|---|---|---|
| **딥 퍼플** | `#3E2259` | 주 버튼, 헤더, 활성 표시, 찜 |
| **브라이트 퍼플** | `#5A3580` | 주 버튼 그라데이션의 밝은 쪽 |
| **호버 퍼플** | `#52306F` | 버튼 hover |
| **미디엄 퍼플** | `#856EA6` | 섹션 머리말, 보조 강조 |
| **라이트 퍼플** | `#A28EBF` | 어두운 사진 위 텍스트 (퍼플은 여기서 1.48:1 로 사라진다) |

### 금색 — 등급 표시다, 장식이 아니다

금색은 **원작에만** 쓴다. 이것이 이 시스템의 유일한 절대 규칙이다.

| 이름 | 값 | 쓰임 |
|---|---|---|
| **골드** | `#C7A15A` | 원작 뱃지의 글씨·테두리 (딥 퍼플 바탕 위, 5.5:1) |
| **골드 딥** | `#A5813D` | 테두리·점 등 장식 요소 |
| **골드 텍스트** | `#876A32` | 밝은 바탕에 얹는 금색 글씨 (`#A5813D` 는 3.5:1 로 안 읽힌다) |

**금색을 써서는 안 되는 자리**: 한정판·오픈에디션 뱃지, 찜 상태, 활성 탭,
섹션 머리말, 초점 링, 아이콘, 테두리 일반. 전에는 이 모두에 금색이 있었고
그래서 금색이 아무 등급도 뜻하지 않았다.

### 중립

Tailwind 기본 회색은 파란 기운이라 퍼플 위에서 겉돈다. 색조만 275° 로 옮기고
명도는 그대로 뒀다 — 명도를 건드리면 본문 보조 글씨의 대비가 사이트 전체에서
같이 움직인다.

| 단계 | 값 | 쓰임 |
|---|---|---|
| background | `#FBFAFC` | 본문 바탕. 순백이 아니다 |
| gray-50 | `#FAF8FB` | |
| gray-100 | `#F4F1F7` | 사진 자리 표시, 옅은 면 |
| gray-200 | `#E7E2EC` | 테두리, 구분선 |
| gray-300 | `#D7CFDD` | 비활성 아이콘 |
| gray-400 | `#A79CAF` | 보조 정보 (어두운 바탕 위 본문) |
| gray-500 | `#776A81` | 설명문, 부제 |
| gray-900 | `#1A1420` | 제목, 가격 |

패널 바탕은 `#F7F5FA` — 원작 구간과 한정판 구간을 층으로 가를 때 쓴다.

**금지**: 순흑 `#000000`. 네온·발광 그림자. 퍼플→골드 그라데이션 (중간이
탁한 갈색을 지나고 끝에서 흰 글씨가 2.4:1 로 떨어진다). 그라데이션은
퍼플 안에서만 움직인다.

---

## 3. 타이포그래피

```
제목(작품·히어로)   Gowun Batang → Nanum Myeongjo → serif   자간 -0.02em
본문               Inter → IBM Plex Sans KR → system         자간 -0.01em
```

명조는 **홈의 히어로와 작품 섹션 제목에만** 쓴다. 나머지는 본문 글꼴 그대로다.
미술품을 거는 자리라 굵은 고딕보다 어울리지만, 남용하면 읽기 어려워진다.

한글 글꼴을 반드시 함께 선언한다. Inter 에는 한글 글자가 없어서, 빼면 운영체제가
주는 것(윈도우면 맑은 고딕)으로 떨어지고 기기마다 다르게 보인다.

가격과 수량은 `tabular-nums`. 자릿수가 흔들리면 목록이 어지럽다.

### 크기

| 자리 | 값 |
|---|---|
| 섹션 제목 | `text-3xl` → `md:text-[34px]`, `font-bold`, 명조 |
| 작품 이름 | `text-[17px]` → `md:text-[19px]`, `font-bold`, 명조 |
| 가격 | `text-[15px]` → `md:text-base`, `font-medium`, tabular |
| 설명문 | `text-sm`, gray-500 |
| 머리말 라벨 | `text-[10px]`~`text-[11px]`, `tracking-[0.24em]`, uppercase |
| 작가명 | `text-[13px]`, gray-400 |

한글은 `break-keep` 을 건다. 없으면 단어 중간에서 줄이 끊긴다.

> **알아둘 것** — 이 스킬은 `Inter` 를 금지하지만 KOALA 는 이미 Inter 로 배포돼
> 있다. 바꾸려면 `Satoshi` 나 `Geist` 로 옮기는 게 맞고, 그때 한글 짝(IBM Plex
> Sans KR)은 그대로 두면 된다. **지금 바꾸지 않은 것은 결정이 아니라 보류다.**

---

## 4. 컴포넌트

### 버튼

주 버튼은 퍼플 그라데이션에 안쪽 하이라이트를 얹는다.

```
bg-gradient-to-r from-koala-purple to-koala-purple-bright
text-white text-base font-bold rounded-xl py-4
shadow-[inset_0_1px_0_rgba(255,255,255,0.12)]
transition-[filter,transform] duration-300 ease-[cubic-bezier(0.32,0.72,0,1)]
hover:brightness-[1.12]  active:scale-[0.985]
disabled:opacity-50 disabled:cursor-not-allowed
```

hover 에서 색을 갈아끼우지 않고 밝기만 올린다 — 그라데이션이 있으면 색 교체가
튄다. `active:scale` 로 눌리는 느낌만 준다. **바깥 발광 금지.**

보조 버튼은 `border border-gray-200` 에 글자만. 세 번째 등급이 필요하면
버튼을 늘리지 말고 밑줄 링크(`border-b border-gray-400 pb-0.5 text-[13px]`)를 쓴다.

### 작품 카드

두 가지 몸이 있다. 겉모습만 다르고 안쪽(클릭 시 열리는 상세 모달, 장바구니,
찜)은 같다.

- **store** — 사진 위에 뱃지를 얹는 목록용
- **editorial** — 사진 아래에 마크·이름·작가·가격을 세로로 쌓는 홈용

비율은 자리에 따라 다르다: 큰 카드 `aspect-square`, 격자 `aspect-[4/5]`,
목록 `aspect-[3/4]`. 사진이 카드보다 커야 작품이 산다.

**뱃지 위계** — 같은 퍼플 바탕 위에서 금색이 위, 흰색이 아래다.

```
원작       퍼플 그라데이션 + 금색 글씨 + 금색 테두리 + 비스듬한 광택
한정판     퍼플 단색 + 흰 글씨
분류       흰 바탕(90%) + 회색 글씨
```

찜 버튼은 사진 위가 아니라 **이름 옆**에 둔다. 작품을 가리지 않는다.

### 가격 표기 — 틀리면 돈이 틀어진다

DB 의 `listPrice`·`salePrice` 는 **부가세를 뺀 공급가액**이다. 화면에는
**반드시 `displayPrice`** 를 쓴다. 잘못 쓰면 표시가와 결제가가 10% 어긋난다.
`taxExempt` 인 원작에는 부가세가 붙지 않아 둘이 같다.

### 카드 · 면

그림자는 거의 쓰지 않는다. 구분이 필요하면 `border-gray-200` 한 줄이나
바탕 톤(`#F7F5FA`)으로 층을 나눈다. 그림자를 쓸 때는 배경 색조를 섞는다 —
검정 그림자는 이 팔레트에서 튄다.

모서리는 `rounded-xl` 을 기본으로 하되 **작품 사진은 각지게 둔다.** 액자에
둥근 모서리를 주지 않는 것과 같다.

### 상태

- **로딩** — 실제 배치와 같은 크기의 회색 면(`animate-pulse bg-gray-100`). 원형 스피너 금지
- **빈 상태** — "없습니다" 한 줄로 끝내지 않는다. 무엇을 하면 채워지는지 적는다
- **오류** — 필드 아래 인라인. `alert()` 금지

---

## 5. 레이아웃

```
컨테이너   mx-auto max-w-[1320px]
좌우 여백   px-5  →  md:px-10
세로 여백   py-16 →  md:py-24
```

### 작품 진열

넓은 화면은 격자, **좁은 화면은 한 줄로 펴서 옆으로 민다.**

```
< 768px    한 줄 가로 스크롤 · 스냅 · 카드 62vw (큰 것 78vw)
≥ 768px    2열 격자
≥ 1024px   4열 격자
```

모바일에서 격자로 두면 폭을 반씩 나눠 가져 카드가 손톱만 해진다. 작품을 보러
온 사람에게 사진이 작아지는 것은 그대로 손해다.

가로 스크롤에는 **반드시 `scroll-px-5`** 를 건다. 없으면 스냅이 좌우 여백을
건너뛰고 붙어 첫 카드가 제목선보다 20px 왼쪽에서 시작한다.

카드 폭에는 `md:max-w-none` 을 함께 건다. `md:w-auto` 는 width 만 풀고
max-width 는 그대로 둬서, 넓은 화면에서 카드가 칸보다 작게 찌그러진다.

### 화면 크기로 갈라야 할 때

CSS 로 숨기지 않고 `useIsWide()`·`useIsDesktop()` 로 **렌더 자체를 가른다.**
CSS 로만 숨기면 DOM 에 남아 모바일에서도 PC 용 사진을 전부 내려받는다.
상품 사진이 평균 532KB 다.

### 금지

전면 폭을 넘는 가로 스크롤. 겹치는 요소. 같은 크기 카드 3열. `h-screen`
(iOS 사파리에서 주소창이 접히며 튄다 — `min-h-[100dvh]` 또는 `svh` 를 쓴다).

---

## 6. 모션

스프링을 기본으로 한다. 선형 이징은 쓰지 않는다.

```
사진 hover        scale 1.03, duration 0.5
버튼 press        scale 0.985, cubic-bezier(0.32, 0.72, 0, 1)
탭 전환           spring, stiffness 420
목록 등장         0.45s cubic-bezier(0.22, 0.61, 0.36, 1), nth-child 로 계단 지연
```

`transform` 과 `opacity` 만 움직인다. `top`·`left`·`width`·`height` 는
건드리지 않는다.

`prefers-reduced-motion` 을 반드시 존중한다. 이미 `.auth-stagger` 와
`.koala-bm-swing` 에 걸려 있다.

**무한 반복 애니메이션은 쓰지 않는다.** 작품 옆에서 무언가 계속 움직이면
시선이 그리로 간다.

---

## 7. 접근성

- 본문 글씨 대비 **4.5:1** 이상. 어두운 사진 위에서는 색을 바꾸지 말고
  **밝은 쪽 토큰**으로 옮긴다 (딥 퍼플 → 라이트 퍼플, 또는 흰색)
- 아이콘만 있는 버튼에는 `aria-label`. 찜 버튼에는 `aria-pressed`
- 초점 링을 지우지 않는다. `focus-visible:outline-2 focus-visible:outline-koala-purple`
- 작품 사진 `alt` 는 "○○ 작 △△" 형태로 작가와 작품을 함께 적는다

---

## 8. 글

- 느낌표를 쓰지 않는다. "저장되었습니다!" 가 아니라 "저장했습니다"
- "앗!", "이런!" 을 쓰지 않는다. 무엇이 잘못됐고 어떻게 하면 되는지 적는다
- 버튼 글자는 무슨 일이 일어나는지 그대로 쓴다 — "확인" 이 아니라 "주문하기"
- 분류 이름은 코드가 아니라 **DB 값**을 쓴다. 화면마다 적어 두면 어드민에서
  바꿔도 한쪽만 바뀐다

---

## 9. 하지 않는 것

**색**
순흑 `#000000` · 네온·발광 그림자 · 퍼플→골드 그라데이션 · 원작 아닌 곳의 금색 ·
채도 80% 이상 accent · 따뜻한 회색과 차가운 회색 섞어 쓰기

**타이포**
장식용 명조 남용 · 한글 글꼴 선언 누락 · 12px 미만 본문 · 큰 제목에 그라데이션 글씨

**레이아웃**
겹치는 요소 · 같은 크기 카드 3열 · `h-screen` · 가운데 정렬 히어로 ·
`calc()` 백분율 계산 · 모바일 가로 넘침

**모션**
무한 반복 · 선형 이징 · `width`/`height` 애니메이션 · `prefers-reduced-motion` 무시

**콘텐츠**
이모지 · "홍길동"·"Acme" 같은 자리표시 이름 · `99.99%` 같은 꾸민 숫자 ·
"경험하세요"·"혁신적인"·"차원이 다른" 같은 빈 말 · "아래로 스크롤" 안내 ·
튀는 화살표 · 깨진 이미지 링크

**데이터**
고객 화면에 `salePrice` 직접 노출 (반드시 `displayPrice`) ·
분류 이름 하드코딩 · 재고를 숫자 한 칸으로 다루기(장부 합계다)

---

## 10. Stitch 에 넘길 때

새 화면을 만들 때 이 문장을 앞에 붙인다.

> 한국 작가 미술품 마켓의 화면이다. 바탕 `#FBFAFC`, 브랜드 딥 퍼플 `#3E2259`,
> 중립은 275° 로 기운 회색 계열. 금색 `#C7A15A` 는 원작 표시에만 쓰고 다른
> 어디에도 쓰지 않는다. 제목은 명조(Gowun Batang), 본문은 산세리프.
> 작품 사진을 크게 걸고 화면 요소는 조용히 둔다. 컨테이너 1320px,
> 좌우 여백 20px(데스크톱 40px). 모바일에서 작품은 한 줄 가로 스와이프,
> 768px 부터 2열, 1024px 부터 4열. 이모지·네온·발광 그림자·같은 크기 3열
> 카드·가운데 정렬 히어로는 쓰지 않는다.
